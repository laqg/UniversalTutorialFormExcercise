const base_url = "https://www.universal-tutorial.com/api";

const routes = {
  GET_TOKEN: `${base_url}/getaccesstoken`,
  GET_STATES: `${base_url}/states/United States`,
  GET_CITIES: `${base_url}/cities/`
};

export default routes;
